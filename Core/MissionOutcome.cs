namespace GTAMissions.Core
{
    /// <summary>
    /// The result a mission ends with.
    /// </summary>
    public enum MissionOutcome
    {
        Success,
        Failed,
        Cancelled
    }
}
